<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PrimeiroXamarin.Views.MainPage"
             Title="{Binding Title}">

    <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
        <Label Text="{Binding Saida}"  FontSize="Large"/>
        <Entry Text="{Binding Entrada}"></Entry>
        <Button Text="{Binding NomeBotao}" Command="{Binding BotaoOKCommand}"></Button>
        
        <!-- NOVIDADES -->

        <Button Text="Listar Todos"  Command="{Binding BotaoListarTodosCommand}" />
<!--
        <Button Text="Listar Aluno"  Command="{Binding BotaoListarEspecificoCommand}" />

        <Button Text="Atualizar Aluno"  Command="{Binding BotaoAtualizarAlunoCommand}" />

        <Button Text="Remover Aluno"  Command="{Binding BotaoRemoverAlunoCommand}" />

        <Button Text="Inserir Aluno"  Command="{Binding BotaoInserirAlunoCommand}" />
-->

        <!-- NOVIDADES -->
    </StackLayout>

</ContentPage>